import './index.css';

const ArticlesNotFound = () => {
    return (
        <div className="no-articles">
            <h1 className="noArticles_main-title">Oops!!</h1>
            <h4 className="noArticles_sub-title">No articles found!</h4>
        </div>
    );
};

export default ArticlesNotFound;